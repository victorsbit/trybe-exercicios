function toUpperCase(str) {
  return str.toUpperCase();
}

function firstLetter(str) {
  return str[0];
}

function strConcat(a, b) {
  return `${a}${b}`;
}

module.exports = { toUpperCase, firstLetter, strConcat };
